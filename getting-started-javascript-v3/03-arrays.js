/**
 * Exercise 3: Arrays
 * Section: 07 - Advanced Variables
 *
 * Task:
 * Create an array with the first 5 letters of the alphabet, then print out
 * the middle element (which should be "C").
 */

const letters = ["A", "B", "C", "D", "E"]

// The middle element is at index 2 (remember: arrays start counting at 0)
console.log(letters[2]) // "C"
